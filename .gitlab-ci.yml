

stages:
  - deploy

pages:
  stage: deploy
  image: python:latest
  script:
    - pip install poetry
    - poetry install
    - poetry run zensical build --clean
  pages:
    publish: public
  rules:
#    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    - if: '$CI_COMMIT_TAG'
